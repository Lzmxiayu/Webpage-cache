# Webpage Cache（高并发网页快照系统）

## 一、项目简介

Webpage Cache 是一个基于 Go 构建的生产级高并发网页截图系统。

本项目采用异步任务架构，结合 Redis 任务队列、Worker Pool
并发控制机制以及浏览器实例池优化策略，实现高吞吐、可扩展、可恢复的截图服务。

------------------------------------------------------------------------

## 二、系统架构

### 总体架构图

``` mermaid
flowchart TD
    Client --> API_Server
    API_Server --> Redis_Queue
    Redis_Queue --> Worker_Cluster
    Worker_Cluster --> Browser_Pool
    Worker_Cluster --> MySQL
    Worker_Cluster --> Object_Storage
```
[详细说明](./架构设计.md)

------------------------------------------------------------------------

## 三、核心架构设计

### 1️⃣ 异步任务模型

截图属于耗时操作（CPU + IO 密集）。

若采用同步 HTTP 处理模式，可能导致：

-   HTTP 长连接阻塞
-   Goroutine 堆积
-   内存占用暴涨
-   服务不可用

本系统采用：

1.  提交任务立即返回 task_id
2.  后台异步执行
3.  客户端轮询获取结果

优势：

-   支持高并发
-   提高系统吞吐
-   降低请求阻塞风险

------------------------------------------------------------------------

### 2️⃣ Worker Pool 并发控制

采用固定大小的 Worker 池：

-   限制最大并发截图数量
-   避免 CPU 被打满
-   提供系统级背压控制

设计目标：

-   可预测的资源使用
-   可控的系统负载
-   平滑处理流量峰值

------------------------------------------------------------------------

### 3️⃣ Redis 任务队列解耦

Redis 用作任务队列，实现 API 层与执行层解耦。

优点：

-   支持多 Worker 实例横向扩展
-   提高系统可用性
-   可替换为 Kafka / RabbitMQ（通过接口抽象）

------------------------------------------------------------------------

### 4️⃣ 浏览器实例池优化

频繁创建浏览器进程开销极高。

系统通过维护浏览器实例池：

-   复用 Chrome 实例
-   降低启动成本
-   减少 CPU 抖动
-   提高整体吞吐量

------------------------------------------------------------------------

### 5️⃣ 任务状态机设计

任务状态流转：

    pending → processing → done
                         ↘ failed

支持：

-   异常恢复
-   超时重试
-   幂等处理
-   崩溃后重入队机制

------------------------------------------------------------------------

## 四、项目结构

    cmd/
        server/
    internal/
        api/
        service/
        repository/
        worker/
        queue/
        browser/
        model/
    config/
    deployments/

------------------------------------------------------------------------

## 五、可扩展性设计

系统支持水平扩展：

-   API 层无状态
-   Worker 可多实例部署
-   共享 Redis 与数据库
-   可线性提升处理能力

扩展方式：

-   增加 Worker 实例
-   增加机器资源
-   使用分布式缓存

------------------------------------------------------------------------

## 六、可靠性保障

-   任务持久化存储
-   超时检测与重试机制
-   队列长度限制防止过载
-   优雅停机支持
-   幂等任务处理设计

------------------------------------------------------------------------

## 七、代理池接入

为应对部分网站反爬限制，系统已支持“代理池 + 重试换代理”。

### 工作机制

- 通过 `PROXY_URLS` 配置多个代理地址（逗号分隔）
- Browser Pool 初始化实例时按轮询分配代理
- 任务失败重试时，会优先避开上一次使用的代理

### 关键配置

- `PROXY_URLS`：代理地址列表
- `BROWSER_POOL_SIZE`：Chrome 实例数
- `MAX_TABS_PER_BROWSER`：每实例并发 tab 数

总截图并发上限约为：

`BROWSER_POOL_SIZE * MAX_TABS_PER_BROWSER`

### 配置示例

```bash
PROXY_URLS=http://user:pass@proxy1:8080,http://user:pass@proxy2:8080
BROWSER_POOL_SIZE=2
MAX_TABS_PER_BROWSER=3
```

如果不需要代理，将 `PROXY_URLS` 置空即可（默认直连）。

------------------------------------------------------------------------


## 八、技术栈

-   Go
-   Gin
-   Redis
-   MySQL
-   chromedp

------------------------------------------------------------------------

## 九、部署

### 本地运行

``` bash
go mod tidy
go run cmd/server/main.go
```

### Docker部署

[README_Docker](./deployments/README_Docker.md)

------------------------------------------------------------------------

## 十、未来优化方向
待补充

------------------------------------------------------------------------

## License

MIT